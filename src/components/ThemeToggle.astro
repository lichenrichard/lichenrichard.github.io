<button class="theme-toggle" type="button" role="switch" aria-label="Dark mode" aria-checked="false" title="Switch to dark mode" data-theme-toggle hidden>
  <svg class="sun" aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round"><circle cx="12" cy="12" r="3.5" /><path d="M12 2v2m0 16v2M2 12h2m16 0h2M5 5l1.5 1.5m11 11L19 19M5 19l1.5-1.5m11-11L19 5" /></svg>
  <svg class="moon" aria-hidden="true" width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.7" stroke-linecap="round" stroke-linejoin="round"><path d="M20.5 14.1A8.5 8.5 0 0 1 9.9 3.5a8.5 8.5 0 1 0 10.6 10.6Z" /></svg>
</button>

<style>
  .theme-toggle { position: relative; display: flex; align-items: center; justify-content: space-around; flex: 0 0 68px; width: 68px; height: 44px; padding: 4px; border: 1px solid var(--control-border); border-radius: 999px; background: var(--paper); color: var(--muted); }
  .theme-toggle::before { content: ''; position: absolute; left: 5px; top: 6px; width: 30px; height: 30px; border-radius: 50%; background: var(--soft); transition: transform .18s ease; }
  .theme-toggle[aria-checked="true"]::before { transform: translateX(26px); }
  .theme-toggle svg { position: relative; flex-shrink: 0; }
  .theme-toggle[aria-checked="false"] .sun, .theme-toggle[aria-checked="true"] .moon { color: var(--accent); }
  .theme-toggle:hover { border-color: var(--accent); }
  @media (prefers-reduced-motion: reduce) { .theme-toggle::before { transition: none; } }
  @media (forced-colors: active) {
    .theme-toggle[aria-checked="false"] .sun, .theme-toggle[aria-checked="true"] .moon { outline: 2px solid Highlight; border-radius: 50%; }
  }
</style>
